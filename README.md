# Terminal_Q — хаб офіційних сторінок розробника

Цей репозиторій — майбутній **хаб юридичних/публічних сторінок розробника**
(Privacy Policy, Terms of Service, Security, Support, About), на які зможе
посилатись будь-який ІТ-проєкт, що публікується під брендом **Terminal_Q**.
Перший проєкт, який буде підключено — `ai-bridge-site`, локально в
`../ai-bridge-site`. Продукт зараз задеплоєний на
`https://ai-bridge-beta.legotron.chatgpt.site` — назва "Artifacto Bridge"
**вилучена**, "Bridge" також визнано занадто загальною назвою продукту.
Нова назва продукту ще не вибрана.

> Посилання на `ai-bridge-site` нижче — відносні (`../ai-bridge-site`) і
> працюють лише локально, поки обидва проєкти лежать поряд на диску. Коли
> буде відомий GitHub owner/repo цього хабу й `ai-bridge-site`, замінити на
> абсолютні canonical URL (репозиторію та/або задеплоєного сайту).

**Поточний фокус — виключно бюрократично-організаційні речі**: юридичні
сторінки, бренд розробника, GitHub-профіль. Сам продукт `ai-bridge-site`
(код, UI, назва, задеплоєна beta) в рамках цього репозиторію не чіпається.

Документ фіксує рішення, прийняті в розмові, і план подальших кроків, щоб
наступна сесія (людина чи інша модель) могла продовжити без перепитувань.

## Статус: рішення зафіксовані

| Питання | Рішення |
|---|---|
| Бренд розробника (парасолька над проєктами) | **Terminal_Q** |
| Юридична назва на сторінках | **Terminal_Q** — офіційний бренд розробника-як-компанії. Юрособа: ФОП, один власник = директор = розробник (див. "Юридична структура" нижче). Публічно — лише прізвище + ініціали, без ПІБ повністю |
| Контактний email | Перехідний стан: зараз у файлах хабу `elakstron@gmail.com`, планується заміна на **`terminalq.dev@gmail.com`** (новий Google-акаунт під бренд). **Заміну в HTML-файлах ще не зроблено** — чекає підтвердження, що акаунт `terminalq.dev@gmail.com` створено й перевірено |
| Розділення акаунтів | На новий акаунт `terminalq.dev@gmail.com` переносяться **тільки проєкти, що публікуються** під брендом Terminal_Q; інше лишається на особистому акаунті користувача. Створено окремий профіль браузера під `terminalq.dev@gmail.com` |
| Де живуть юридичні сторінки | Окремий хаб-репозиторій (цей), незалежний від `ai-bridge-site` — бо планується багато різних проєктів |
| Хостинг хабу | GitHub Pages |
| GitHub-акаунт розробника | Персональний акаунт (не org) |
| Аудиторія монетизації `ai-bridge-site` | Планується міжнародна |
| Назва бренду `ai-bridge-site` ("Artifacto", "Bridge") | **Вилучена / визнана занадто загальною** — нова назва продукту не вибрана, продукт зараз не перейменовуємо |
| Назви решти проєктів (RAG, coding tooling, browser translation) | **Ще не вирішено** — окремий крок після хабу |
| GitHub username | **`terminal-q`** — зафіксовано, перевірено вільним 2026-09-01 (`TerminalQ` зайнятий, `_` у логінах GitHub не допускається). Реєстрація нового окремого акаунту **в процесі**, з браузерного профілю під `terminalq.dev@gmail.com` |
| Модель GitHub Pages / URL хабу | **User-site**: репозиторій `terminal-q.github.io`, шляхи без префіксу (`/privacy`, `/terms`, ...) |

## Юридична структура

- Бренд **Terminal_Q** = один юридичний власник = директор = розробник, одна й та сама людина.
- Юрособа: ФОП, 2 група, Україна.
- ПІБ (внутрішній запис, **не для публікації повністю**): Чернієнко Олександр Іванович.
- Публічна форма на будь-якій сторінці/футері: **«ФОП Чернієнко О. І.»** (прізвище + ініціали).

### Політика мінімального розкриття персональних даних

За замовчуванням на публічних сторінках **не** публікуються: повне ім'я по
батькові, домашня/юридична адреса, телефон, дата народження, номер
паспорта/ІПН чи інші державні ідентифікатори. Дозволено: прізвище + ініціали,
контактний email (`elakstron@gmail.com`), назва бренду.

Виняток — якщо конкретна платформа публікації (Stripe, App Store, ChatGPT
Apps, банк-еквайр тощо) технічно вимагає повних реквізитів для верифікації —
тоді розширений набір даних надається саме туди (у формі платформи), а не
виставляється на публічній сторінці хабу.

## ⚠️ Юридичний застережний пункт — ФОП 2 групи

Користувач має ФОП 2 групи в Україні та планує **міжнародну монетизацію**
(ChatGPT Apps / іноземні картки).

Для ФОП 2 групи існують обмеження щодо того, кому можуть надаватися послуги:
платникам єдиного податку та/або населенню. Для міжнародної SaaS/marketplace-
моделі потрібно окремо визначити, хто юридично є замовником і платником —
кінцевий користувач, маркетплейс чи інша компанія.

**Дія перед запуском платежів:** перевірити цю модель з бухгалтером/податковим
консультантом; за потреби розглянути 3 групу. Terms/Privacy тексти в цьому
хабі написані **нейтрально**, без прив'язки до конкретної групи ФОП, щоб не
потребувати переписування після цього рішення. Це не юридична консультація.

## Сторінки хабу (план)

Мінімальний "повний фарш" для публікації й монетизації будь-якого проєкту.
Шляхи нижче (`/privacy`, `/terms`, ...) — умовні, кінцевого вигляду набудуть
залежно від deployment-моделі:

- **Обрано: user-site** `terminal-q.github.io` → шляхи такі, як записано (`/privacy`, `/terms`, ...), без префіксу.
- Це вимагає, щоб репозиторій на GitHub називався **точно** `terminal-q.github.io` — інакше GitHub Pages піде в project-site режим із префіксом `/offsite/...`.
- До моменту фактичного push на GitHub локальна папка лишається `D:\Kod\offsite` — назва локальної теки не впливає на URL, важлива тільки назва remote-репозиторію.

- `/` — головна / про розробника (аналог наявного `app/developer/page.tsx` в `ai-bridge-site`, але як канонічне джерело)
- `/privacy` — Privacy Policy (яких даних торкається, як зберігаються/видаляються, GDPR-подібні права)
- `/terms` — Terms of Service (умови використання, відповідальність, обмеження гарантій)
- `/security` — Security Policy / Responsible Disclosure (як повідомити про вразливість, SLA на відповідь)
- `/support` — Support / Contact (email, очікуваний час відповіді, канали)
- `/refunds` — Refund / Cancellation Policy (додається разом із першою платною підпискою)
- `/cookies` — Cookie Policy (додається, якщо на сайтах з'являється аналітика/трекінг)

Кожна сторінка — двомовна (uk/en), за зразком i18n-патерну з
`ai-bridge-site` (`components/public/i18n.ts`, `COPY` record).

## Дорожня карта (поступово)

- [x] Фаза 0 — назва бренду розробника: **Terminal_Q**
- [ ] Фаза 0.5 — **[дія користувача]** зафіксувати GitHub username `terminal-q` (реєстрація/перейменування акаунту) і створити порожній репозиторій **точно** з назвою `terminal-q.github.io`
- [x] Фаза 1 — каркас хабу зроблений локально в `D:\Kod\offsite`: `index.html`, `privacy/`, `terms/`, `security/`, `support/`, спільні `assets/style.css` + `assets/i18n.js` (uk/en перемикач, детекція мови браузера, збереження в localStorage). Перевірено локальним прев'ю-сервером — рендериться коректно, консоль без помилок
- [ ] Фаза 1.5 — **[дія користувача + спільно]** підключити `D:\Kod\offsite` як git-репозиторій, remote → `terminal-q.github.io`, перший push; увімкнути GitHub Pages (для user-site зазвичай автоматично)
- [ ] Фаза 2 — верифікований GitHub-профіль розробника (bio, аватар, pinned repos, підтверджений email) — **[дія користувача]**
- [ ] Фаза 3 — `ai-bridge-site` посилається на хаб-сторінки замість заглушки в `/developer` (контактна секція там прямо очікує на "confirmed public info")
- [ ] Фаза 4 — назви окремих продуктів під брендом Terminal_Q (`ai-bridge-site` без назв "Artifacto"/"Bridge", RAG-система, coding tooling, browser translation tooling)
- [ ] Фаза 5 — легалізація монетизації (бухгалтер, можливо перехід на 3 групу ФОП) → підключення реальних платежів

## Відкриті питання для наступного кроку

1. Яка нова назва продукту `ai-bridge-site` замість "Artifacto Bridge"/"Bridge"? (окремий крок, не зараз)
2. ~~Кастомний домен для хабу, чи `<username>.github.io`?~~ — вирішено: user-site `terminal-q.github.io`, кастомний домен можна додати пізніше поверх нього без зміни структури сторінок.
